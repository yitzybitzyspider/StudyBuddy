"""StudyBuddy: a personal-first adaptive study system.

A deterministic pipeline scopes and drives Claude (the semantic engine). The knowledge
layer (concepts, items, prompts, heuristics, runs, learner) is the product; this package
is the disposable glue that reads and writes it. See docs/ for the authoritative design.
"""

__version__ = "0.0.0"
