# Copy to .env and fill in. The Claude-call wrapper reads ANTHROPIC_API_KEY from the environment.
ANTHROPIC_API_KEY=

# Optional: override the default model used by the wrapper.
# STUDYBUDDY_MODEL=claude-opus-4-8
